<!--
  This repository gates on its own protocol. The AI-use disclosure below is not a
  formality: a check fails without it (SCPE level 1). Tick exactly one box, or add an
  Assisted-by trailer to a commit message instead — either form satisfies the gate.
-->

## What this changes

<!-- One or two sentences. What is different after this merge, and why. -->

## AI use

<!-- Tick ONE. Both answers are welcome; only silence fails the check. -->

- [ ] I used generative AI for part of this change
- [ ] I did not use generative AI for this change

<!--
  If you used AI, naming the tool helps a reviewer calibrate — e.g. "used it for the
  test scaffolding, hand-written elsewhere". Put that in the line above, or use a
  commit trailer in the form: Assisted (hyphen) by: <tool name>

  Note for whoever edits this template: do NOT write that trailer at the start of a
  line anywhere in this file, comments included. The checker scans the raw PR body and
  does not skip HTML comments, so an example trailer here would satisfy the gate for
  every PR that leaves the template untouched.

  A disclosure is not a confession. This project takes the position that AI-assisted
  contributions are normal and welcome; what it refuses is the question going
  unanswered.
-->

## Checks

- [ ] Tests pass locally, or this change carries no code
- [ ] I read [CONTRIBUTING.md](../CONTRIBUTING.md)
